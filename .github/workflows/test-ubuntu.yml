name: Test on Ubuntu

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  build:
    uses: ./.github/workflows/reusable-distro-build.yml

  test:
    needs: build
    uses: ./.github/workflows/reusable-distro-test.yml
    with:
      distro: ubuntu
      lxd_image: ubuntu:lts
      snap_artifact_name: built-snap
