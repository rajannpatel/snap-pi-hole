--- a/src/files.c
+++ b/src/files.c
@@ -435,8 +435,7 @@
 		pwd = getpwnam("pihole");
 		if(pwd == NULL)
 		{
-			log_warn("chown_pihole(): Failed to get pihole user's UID/GID: %s", strerror(errno));
-			return false;
+			return true;
 		}
 	}
 
