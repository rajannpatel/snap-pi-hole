#!/bin/sh
# Runs once at first install. The `layout:` block aliases these paths to
# $SNAP_DATA / $SNAP_COMMON, so creating them here populates the right
# writable backing directories.
set -eu

mkdir -p /etc/pihole /etc/dnsmasq.d /run/pihole /var/log/pihole
